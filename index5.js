let name = 'Mosh';
let age = 30;
let isApproved = false;
let firstName = undefined;
let selectedColor = null;

// what happens is that we can typeof on any of these....so typeof name shows string. if name = 1, we typeof name again and it shows "number". This is because the type changed dynamically to fit the new assignment.

